/**
 * Card treatment shared by the featured hero chip and the open experience row:
 * white surface, stone hairline, soft shadow, 0.75rem radius (tokens in
 * globals.css). Pair chips reuse the surface/hairline/radius without the shadow.
 */
export const cardClass =
  "rounded-card border border-hairline bg-surface shadow-card";

export const flatCardClass = "rounded-card border border-hairline bg-surface";
