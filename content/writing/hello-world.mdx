export const meta = {
  title: "Hello world",
  date: "2026-09-07",
  summary: "Pipeline fixture. Kept as a draft so it never lists or indexes.",
  draft: true,
};

# Hello world

This file exists so `pnpm build` compiles an MDX post. It stays `draft: true`
until there is something worth publishing.

- Drafts are excluded from `/writing`, the homepage, RSS, and the sitemap.
- Hitting this slug directly 404s.
